version: "3.8"

services:
  frontend:
    build : ./frontend
    container_name: react-ui
    ports:
      - 5173:5173
    stdin_open : true
    tty : true
    depends_on:
      - backend 
    networks:
      - mern-networks

  backend :
    build: ./backend
    environment: 
      - PORT=4000
      - user= jrragh@gmail.com
      - pass = mfyz uqvw yopi dbdf
      - key_id= rzp_test_w5gM0MlnOtBwFl
      - key_secret = u71SDuXyKWqpRa0cW5nU6paN
      - JWT_SECRET=ragh

    container_name: node-api
    restart : always
    ports:
      - 4000:4000
    depends_on:
      - database
    networks:
      - mern-networks
  
  database:
    image: mongo
    container_name: mongo-db
    ports:
      - 27017-27017
    volumes:
      - data-volume:/data/db
    networks:
      - mern-networks

volumes:
  data-volume:
    driver: local
    
          
networks:
  mern-networks:
    driver: bridge

    
